Create a directory here called mongo and copy in to it the mongod binary from:

https://fastdl.mongodb.org/linux/mongodb-linux-x86_64-latest.tgz

Then execute createimage.sh

Then enter build/ directory and run

`docker build -t clusterhq/mongodb .`

to build the final image

then `docker run clusterhq/mongodb` to test.
